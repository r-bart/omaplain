"""Core library for OmaPlain."""

# Una sola verdad sobre la versión: la del `manifest.json`, que es la que el
# shell lee. Un test las ata para que no vuelvan a separarse — se separaron
# toda la 0.2.0, y `check-dependencies` decía 0.1.0 a quien preguntara.
__version__ = "0.2.0"

